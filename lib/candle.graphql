

subscription PairCandles($pair: String! $period: Int! )
{
    candles(where: {pair: $pair, period: $period }) {
        id
        time
        pair
        open
        close
        low
        high

    }
}
